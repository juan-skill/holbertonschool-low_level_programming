#include <stdio.h>
/**
* main - prints all possible combinations of single-digit numbers.
*
* Return: Always 0 (Success)
*/
int main(void)
{
int n;
for (n = 48; n < 58; n++)
{
putchar(n);
if (num == 57)
{
putchar('\n');
break;
}
putchar(44);
putchar(32);
}
return (0);
}
