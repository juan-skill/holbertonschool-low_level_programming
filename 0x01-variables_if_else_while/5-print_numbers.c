#include <stdio.h>

/**
 *main - shows all digits in base 10 numbers
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	putchar('\n');
	return (0);
}
