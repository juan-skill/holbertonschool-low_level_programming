#include "holberton.h"

/**
 * _puts - prints a string to stdout
 *
 * @str: string to print
 * Return: void
 */

void _puts(char *str)
{
	int len;

  len = 0;
	while (*s != '\0')
		_putchar(s++);
	_putchar(10);
}
